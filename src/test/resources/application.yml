spring:
  profiles:
    active: test

  data:
    mongodb:
      database: test
      host: localhost
      port: 27017

  jpa:
    hibernate:
      ddl-auto: create-drop

    defer-datasource-initialization: true